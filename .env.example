# Server
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# DeepSeek (OpenAI-compatible) — required for the handover pipeline, not for /health.
# openai SDK -> baseURL https://api.deepseek.com
DEEPSEEK_API_KEY=
